{% extends 'index.html.twig' %}

{% block title %}Edit Accessory{% endblock %}

{% block main %}
    <h1>Edit Accessory</h1>

    {{ include('accessory/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_accessory_index') }}">back to list</a>

    {{ include('accessory/_delete_form.html.twig') }}
{% endblock %}
