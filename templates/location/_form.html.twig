{{ form_start(form) }}
    <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 mb-6">
        {{ form_widget(form) }}
    </div>
    <div class="flex justify-end">
        <button type="submit" class="px-2 py-1 border-2 border-blue-600 rounded font-medium hover:bg-blue-600 hover:text-white">
            {{ button_label|default('Ajouter') }}
        </button>
    </div>
{{ form_end(form) }}
