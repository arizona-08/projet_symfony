{{ form_start(form) }}
<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    <div>
        <label for="{{ form.start_date.vars.id }}" class="block text-sm font-medium text-gray-700 mb-2">
            {{ form.start_date.vars.label }}
        </label>
        {{ form_widget(form.start_date, { 'attr': { 'class': 'w-full px-4 py-2 border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500' } }) }}
    </div>

    <div>
        <label for="{{ form.end_date.vars.id }}" class="block text-sm font-medium text-gray-700 mb-2">
            {{ form.end_date.vars.label }}
        </label>
        {{ form_widget(form.end_date, { 'attr': { 'class': 'w-full px-4 py-2 border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500' } }) }}
    </div>

    <div class="sm:col-span-2 lg:col-span-1">
        <label for="{{ form.vehicle.vars.id }}" class="block text-sm font-medium text-gray-700 mb-2">
            Véhicules
        </label>
        {{ form_widget(form.vehicle, { 'attr': { 'class': 'w-full px-4 py-2 border-gray-300 rounded-md shadow-sm focus:ring-blue-500 focus:border-blue-500' } }) }}
    </div>
</div>

<div class="flex justify-end mt-4">
    <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded-md hover:bg-blue-700">
        {{ button_label|default('Enregistrer') }}
    </button>
</div>
{{ form_end(form) }}
